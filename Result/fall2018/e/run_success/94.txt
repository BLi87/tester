SELECT BAR FROM FREQUENTS WHERE FREQUENTS.DRINKER = 'Ben' or FREQUENTS.DRINKER = 'Dan' EXCEPT (SELECT BAR FROM FREQUENTS WHERE FREQUENTS.DRINKER = 'Ben' AND FREQUENTS.BAR IN (SELECT BAR FROM FREQUENTS WHERE FREQUENTS.DRINKER = 'Dan'));