with servesA as (select * from serves) select barB, barA from (select serves.bar barA, beer, bar.name barB from serves cross join bar except select bar.name barA, beer, serves.bar barB from serves cross join bar) as t1 except select barA, barB from (select serves.bar barA, beer, bar.name barB from serves cross join bar except select bar.name barA, beer, serves.bar barB from serves cross join bar) as t2;