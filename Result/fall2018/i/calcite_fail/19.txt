SELECT name FROM Drinker WHERE name NOT IN(SELECT drinker FROM ((SELECT drinker, bar, beer FROM Frequents NATURAL JOIN Serves) EXCEPT (SELECT drinker, bar, beer FROM Frequents NATURAL JOIN Serves NATURAL JOIN Likes)) AS d);
