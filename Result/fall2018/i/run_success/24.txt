SELECT DISTINCT name FROM drinker EXCEPT (SELECT DISTINCT drinker FROM (SELECT frequents.drinker, frequents.bar, serves.beer FROM frequents, serves WHERE frequents.bar = serves.bar EXCEPT (SELECT frequents.drinker, frequents.bar, likes.beer FROM frequents, likes WHERE frequents.drinker=likes.drinker)) a1);