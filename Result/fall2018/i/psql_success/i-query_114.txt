(select name from drinker) except (with wrong as ((select drinker, frequents.bar, beer from frequents join serves on frequents.bar = serves.bar) except (select frequents.drinker, frequents.bar, beer from frequents join (select distinct drinker, serves.bar, serves.beer from likes join serves on likes.beer = serves.beer) i on frequents.bar = i.bar and frequents.drinker = i.drinker)) select distinct drinker from wrong);