SELECT DISTINCT name FROM drinker EXCEPT (SELECT DISTINCT drinker from (SELECT DISTINCT drinker, bar FROM frequents EXCEPT (SELECT DISTINCT drinker, bar FROM likes, serves WHERE likes.beer=serves.beer)) as foo);