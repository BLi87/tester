(select drinker.name from drinker) EXCEPT ALL (select distinct final.drinker from (select frequents.bar, frequents.drinker from frequents EXCEPT ALL (select distinct c1.s_bar, c1.drinker from (select serves.bar as s_bar, beer, drinker, frequents.bar as f_bar from serves, frequents where serves.bar = frequents.bar) as c1, likes where likes.drinker = c1.drinker and c1.beer = likes.beer)) as final);