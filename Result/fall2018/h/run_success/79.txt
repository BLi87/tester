SELECT NAME FROM DRINKER EXCEPT (SELECT DISTINCT DRINKER FROM (SELECT DRINKER, BAR FROM FREQUENTS EXCEPT (SELECT DISTINCT LIKES.DRINKER, FREQUENTS.BAR FROM LIKES, FREQUENTS, SERVES WHERE LIKES.DRINKER = FREQUENTS.DRINKER AND FREQUENTS.BAR = SERVES.BAR AND SERVES.BEER = LIKES.BEER)) AS FOO);
