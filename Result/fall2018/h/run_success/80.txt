select name from drinker except select drinker as name from (select drinker, bar from frequents except select drinker, bar from (select * from likes, serves where likes.beer = serves.beer) as T) as X;
