select bar,drinker from frequents
	except (select f1.bar, f1.drinker 
		from frequents AS f1, frequents AS f2 
			where f1.drinker<>f2.drinker and f1.bar=f2.bar and 
			f1.times_a_week<f2.times_a_week);