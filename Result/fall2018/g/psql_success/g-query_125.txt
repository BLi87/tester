select distinct bar, drinker from frequents except (select distinct f1.bar, f1.drinker from frequents f1 inner join frequents f2 on not(f1.drinker = f2.drinker) and f1.bar = f2.bar and (f1.times_a_week < f2.times_a_week));
